(function(){
'use strict';
/* Final interception for Regions & territories density buttons. */
if(!window.__rc28RegionViewCapture){window.__rc28RegionViewCapture=true;window.addEventListener('click',e=>{const b=e.target.closest?.('[data-rc18-view]');if(!b)return;const mode=b.dataset.rc18View;if(!['compact','medium','large'].includes(mode))return;e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();store.settings=store.settings||{};store.settings.rc18=store.settings.rc18||{};store.settings.rc18.regionView=mode;saveStore();const grid=document.getElementById('rc18RegionGrid');if(grid){grid.classList.remove('compact','medium','large');grid.classList.add(mode)}document.querySelectorAll('[data-rc18-view]').forEach(x=>x.classList.toggle('active',x===b))},true)}
/* Client queue: use the requested operational wording and exactly five rows by default. */
function queueFix(){if(currentView!=='dashboard'||!currentClient())return;const host=document.getElementById('content');if(!host)return;const panels=[...host.querySelectorAll('.rc11-panel,.rc-v10-panel,.card')];const panel=panels.find(p=>/latest orders requiring action|operational order queue|recent orders/i.test(p.textContent||''));if(!panel)return;const h=panel.querySelector('.rc11-section-head');if(h){const eye=h.querySelector('.eyebrow');const title=h.querySelector('h3');if(eye)eye.textContent='OPERATIONAL ORDER QUEUE';if(title)title.textContent='Latest orders requiring action'}const q=panel.querySelector('.rc11-order-queue,.recent-order-list')||panel;const rows=[...q.querySelectorAll('.rc11-order-line,.recent-order-list>div')].filter(x=>!x.classList.contains('rc28-order-more'));rows.forEach((x,i)=>x.style.display=i<5?'':'none');panel.querySelector('.rc27-order-more')?.remove();panel.querySelector('.rc28-order-more')?.remove();if(rows.length<=5)return;const more=document.createElement('div');more.className='rc28-order-more';more.innerHTML=`<small>${rows.length-5} additional order(s)</small><div class="actions"><button class="btn secondary small" data-q-more>Show all ↓</button><button class="btn primary small" data-q-orders>Open orders</button></div>`;q.after(more);let open=false;more.querySelector('[data-q-more]').onclick=()=>{open=!open;rows.forEach((x,i)=>x.style.display=(i<5||open)?'':'none');more.querySelector('[data-q-more]').textContent=open?'Show first 5 ↑':'Show all ↓'};more.querySelector('[data-q-orders]').onclick=()=>window.rc28Navigate?.('orders')}
const obs=new MutationObserver(()=>setTimeout(queueFix,45));setTimeout(()=>{const c=document.getElementById('content');if(c)obs.observe(c,{childList:true,subtree:true});queueFix()},400);
})();
